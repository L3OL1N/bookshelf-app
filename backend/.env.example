# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend Service
# Set to 'false' to disable serving frontend files (API-only mode)
SERVE_FRONTEND=true

# Database
# SQLite database file location (relative to backend directory)
# DB_PATH=./bookshelf.db

# CORS (if needed for production)
# ALLOWED_ORIGINS=https://your-frontend-domain.com

# Claude API (for AI import feature)
# ANTHROPIC_API_KEY=your_api_key_here
